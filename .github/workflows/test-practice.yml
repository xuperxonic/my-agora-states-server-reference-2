name: test-practice

on:
  push:
    branches: ["reference"] # reference branch에 push를 할 때 실행
  pull_request:
    branches: ["reference"] # reference branch에 PR을 보낼 때 실행

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Bare Minimum Requirements
        uses: actions/setup-node@v1
        with:
          node-version: "16"
      - run: npm install
        working-directory: ./my-agora-states-client
      - run: npm run test
        working-directory: ./my-agora-states-client/src

  deploy: 
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Deploy to Vercel Action
        uses: xuperxonic/my-agora-states-server-reference-2@v1
        with:
          GITHUB_TOKEN: ${{ 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 }}
          VERCEL_TOKEN: ${{ YvHTFsvlTw0fB3yRpBkElyYY}}
          VERCEL_ORG_ID: ${{ 1I12eq3LVXFf1wjJO4wX9oZR }}
          VERCEL_PROJECT_ID: ${{ prj_UkOQz5PDsTLMePh76XO2VvVuOsUT }}
